# server/.env にコピーして値を設定する（server/.env は gitignore 済み）

GOOGLE_SERVICE_ACCOUNT_KEY_FILE=
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# /api/* への共有シークレット認証（未設定の場合、認証は無効化される）。
# Cloud Run 等のインターネット公開環境にデプロイする場合は必須。
# ランダムな値を生成: openssl rand -hex 32
API_SHARED_SECRET=
