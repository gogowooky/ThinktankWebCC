<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" >

    <SolidColorBrush x:Key="GridSplitter_Trigger_Color"         Color="#32CD32"/>

    <SolidColorBrush x:Key="BaseColor"                          Color="#E6E6FA"/>
    <SolidColorBrush x:Key="Label_Title_Background"             Color="#6A5ACD"/>
    <SolidColorBrush x:Key="Label_Title_Foreground"             Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextEditor_Base_Background"         Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextEditor_Base_Foreground"         Color="#483D8B"/>
    <SolidColorBrush x:Key="TextEditor_Keyword_Background"      Color="#FFF0F0"/>

    <SolidColorBrush x:Key="TTEditorInactiveCursorLine"         Color="#E6E6FA"/>
    <SolidColorBrush x:Key="TTEditorActiveCursorLine"           Color="#F5E0F0"/>
    <SolidColorBrush x:Key="TTEditorActiveCursorLine2"          Color="#D5E0FF"/>
    <SolidColorBrush x:Key="TTEditorActiveCursorLine3"          Color="#F4F4E0"/>

    <SolidColorBrush x:Key="TTSelectedFontColor"                Color="#000000"/>
    <SolidColorBrush x:Key="TTDraggingSplitterColor"            Color="#9932CC"/>
    <SolidColorBrush x:Key="TTColumnHeaderColor"                Color="#7B68EE"/>
    <SolidColorBrush x:Key="TTColumnHeaderOnMouseColor"         Color="#C6C6FA"/>
    <SolidColorBrush x:Key="TTColumnHeaderOnMouseFontColor"     Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TTListItemBgColorData"              Color="#EAEAFF"/>
    <SolidColorBrush x:Key="TTListItemFntColorData"             Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TTListItemColorSelected"            Color="#C6C6FA"/>
    <SolidColorBrush x:Key="TTListItemFntColorSelected"         Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TTListItemColorOnMouse"             Color="#D6D6FA"/>
    <SolidColorBrush x:Key="TTListItemFntColorOnMouse"          Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TTScrollBarThumbColor"              Color="#D6D6FA"/>
    <SolidColorBrush x:Key="TTScrollBarBdColor"                 Color="#E6E6FA"/>
    <SolidColorBrush x:Key="TTMenuCaptionColor"                 Color="#FFFFE0"/>
    <SolidColorBrush x:Key="TTMenuCaptionFontColor"             Color="#AAAA90"/>

    <!-- Grid -->
    <Style TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource BaseColor}" />
    </Style>
    <Style TargetType="GridSplitter">
        <Setter Property="Background"   Value="{StaticResource BaseColor}" />
        <Setter Property="Opacity"      Value="0.5" />
        <Setter Property="IsTabStop"    Value="false" />
        <Setter Property="Focusable"    Value="false" />
        <Style.Triggers>
            <Trigger Property="IsDragging"      Value="true" >
                <Setter Property="Background"   Value="{StaticResource GridSplitter_Trigger_Color}" />
            </Trigger>
            <Trigger Property="IsMouseOver"     Value="true" >
                <Setter Property="Background"   Value="{StaticResource GridSplitter_Trigger_Color}" />
            </Trigger>
        </Style.Triggers>            
    </Style>
    <Style x:Key="HSplitterStyle" TargetType="GridSplitter" >
        <Setter Property="HorizontalAlignment"  Value="Stretch" />
        <Setter Property="VerticalAlignment"    Value="Top" />
        <Setter Property="Height"               Value="4" />
        <Setter Property="IsTabStop"            Value="False" />
        <Setter Property="Background"   Value="{StaticResource BaseColor}" />
        <Setter Property="Opacity"      Value="0.5" />
        <Setter Property="Focusable"    Value="false" />
        <Style.Triggers>
            <Trigger Property="IsDragging"      Value="true" >
                <Setter Property="Background"   Value="{StaticResource GridSplitter_Trigger_Color}" />
            </Trigger>
            <Trigger Property="IsMouseOver"     Value="true" >
                <Setter Property="Background"   Value="{StaticResource GridSplitter_Trigger_Color}" />
            </Trigger>
        </Style.Triggers>            
    </Style>
    <Style x:Key="VSplitterStyle" TargetType="GridSplitter" >
        <Setter Property="HorizontalAlignment"  Value="Left" />
        <Setter Property="VerticalAlignment"    Value="Stretch" />
        <Setter Property="Width"               Value="4" />
        <Setter Property="IsTabStop"            Value="False" />
        <Setter Property="Background"   Value="{StaticResource BaseColor}" />
        <Setter Property="Opacity"      Value="0.5" />
        <Setter Property="Focusable"    Value="false" />
        <Style.Triggers>
            <Trigger Property="IsDragging"      Value="true" >
                <Setter Property="Background"   Value="{StaticResource GridSplitter_Trigger_Color}" />
            </Trigger>
            <Trigger Property="IsMouseOver"     Value="true" >
                <Setter Property="Background"   Value="{StaticResource GridSplitter_Trigger_Color}" />
            </Trigger>
        </Style.Triggers>            
    </Style>

    <!-- TTPanel.xaml -->
    <Style x:Key="ScrollBarPageButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{StaticResource TTScrollBarBdColor}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border CornerRadius="0"
                        Background="{StaticResource TTScrollBarThumbColor}"
                        BorderBrush="{StaticResource TTScrollBarThumbColor}"
                        BorderThickness="0" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="VerticalScrollBar" TargetType="{x:Type ScrollBar}">
        <Grid>
            <Track x:Name="PART_Track" IsDirectionReversed="true">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBarThumb}" Margin="0">
                    </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>
    <ControlTemplate x:Key="HorizontalScrollBar" TargetType="{x:Type ScrollBar}">
        <Grid>
            <Track x:Name="PART_Track" IsDirectionReversed="False">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBarThumb}" Margin="0">
                    </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageRightCommand" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>
    <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="12" />
                <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="12" />
                <Setter Property="Height" Value="Auto" />
                <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Editor -->
    <Style TargetType="avalonEdit:TextView">
        <Setter Property="CurrentLineBackground" Value="{StaticResource TTEditorInactiveCursorLine}" />
        <Setter Property="CurrentLineBorder">
            <Setter.Value>
                <Pen Brush="Transparent" Thickness="0"/>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="folding:FoldingMargin">
        <Setter Property="FoldingMarkerBackgroundBrush"         Value="{StaticResource TextEditor_Base_Background}" />
        <Setter Property="FoldingMarkerBrush"                   Value="{StaticResource TTListItemColorSelected}" />
        <Setter Property="SelectedFoldingMarkerBackgroundBrush" Value="{StaticResource TextEditor_Base_Background}" />
        <Setter Property="SelectedFoldingMarkerBrush"           Value="{StaticResource TTListItemColorSelected}" />
    </Style>
    <Style x:Key="KeywordStyle" TargetType="avalonEdit:TextEditor">
        <Setter Property="Background"   Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground"   Value="{StaticResource TextEditor_Base_Foreground}" />
        <Setter Property="FontFamily"   Value="Meiryo"/>
        <Setter Property="FontSize"     Value="10"/>
        <Setter Property="Height"       Value="{Binding ElementName=Title, Path=ActualHeight}" />
        <Setter Property="VerticalScrollBarVisibility"      Value="Hidden" />
        <Setter Property="HorizontalScrollBarVisibility"    Value="Hidden" />
        <Setter Property="HorizontalAlignment"              Value="Stretch" />
    </Style>
    <Style x:Key="EditorStyle" TargetType="avalonEdit:TextEditor">
        <Setter Property="Background"   Value="{StaticResource TextEditor_Base_Background}" />
        <Setter Property="Foreground"   Value="{StaticResource TextEditor_Base_Foreground}" />
        <Setter Property="FontFamily"   Value="Meiryo"/>
        <Setter Property="FontSize"     Value="11"/>
        <Setter Property="HorizontalAlignment"              Value="Stretch" />
        <Setter Property="VerticalAlignment"                Value="Stretch" />
    </Style>

    <!-- DataGrid -->
    <Style TargetType="DataGrid">
        <Setter Property="Focusable"    Value="false" />
        <Setter Property="IsTabStop"    Value="false" />
        <Setter Property="BorderThickness"  Value="0" />
        <Setter Property="Background"   Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground"   Value="{StaticResource TextEditor_Base_Foreground}" />
        <Setter Property="CanUserAddRows"       Value="False" />
        <Setter Property="CanUserDeleteRows"    Value="False" />
        <Setter Property="CanUserResizeRows"    Value="False" />
        <Setter Property="SelectionUnit"        Value="FullRow" />
        <Setter Property="SelectionMode"        Value="Single" />
        <Setter Property="GridLinesVisibility"  Value="None" />
        <Setter Property="RowHeaderWidth"       Value="20" />
        <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
        <Setter Property="EnableColumnVirtualization" Value="True" />
        <Setter Property="EnableRowVirtualization" Value="True" />
    </Style>
    <Style TargetType="DataGridRow">
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource TTColumnHeaderOnMouseColor}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="true" >
                <Setter Property="FontWeight" Value="UltraBold" />
                <Setter Property="Foreground" Value="{StaticResource TTSelectedFontColor}"/> 
               <Setter Property="Background"   Value="{StaticResource TTEditorActiveCursorLine}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background"   Value="{StaticResource TTColumnHeaderColor}" />
        <Setter Property="Foreground"   Value="{StaticResource Label_Title_Foreground}" />
        <Setter Property="HorizontalContentAlignment"   Value="Left"/>
        <Setter Property="VerticalContentAlignment"     Value="Center"/>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver"   Value="true" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"   Value="{StaticResource TTColumnHeaderOnMouseColor}" />
                <Setter Property="BorderBrush"  Value="{StaticResource TTColumnHeaderOnMouseColor}" />
                <Setter Property="Foreground"   Value="{StaticResource TTColumnHeaderOnMouseFontColor}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="DataGridRowHeader">
        <Setter Property="BorderThickness"              Value="0" />
        <Setter Property="HorizontalContentAlignment"   Value="Center" />
        <Setter Property="Background"                   Value="{StaticResource BaseColor}"/> 
        <Setter Property="Foreground"                   Value="{StaticResource TextEditor_Base_Foreground}" />
        <Setter Property="Padding"                      Value="8,0,0,0" />
        <Style.Triggers>
            <Trigger Property="IsRowSelected"   Value="true" >
                <Setter Property="FontWeight"   Value="UltraBold" />
                <Setter Property="Foreground"   Value="{StaticResource TTSelectedFontColor}"/> 
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="DataGridCell">
        <Setter Property="Focusable"        Value="false" />
        <Setter Property="BorderThickness"  Value="0" />
        <Setter Property="IsTabStop"    Value="false" />
        <Setter Property="Background"   Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground"   Value="{StaticResource TextEditor_Base_Foreground}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding _flag}" Value="1">
                <Setter Property="Background" Value="{StaticResource TTEditorActiveCursorLine}"/> 
                <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}"/> 
            </DataTrigger>
            <DataTrigger Binding="{Binding _flag}" Value="2">
                <Setter Property="Background" Value="{StaticResource TTEditorActiveCursorLine2}"/> 
                <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}"/> 
            </DataTrigger>
            <DataTrigger Binding="{Binding _flag}" Value="3">
                <Setter Property="Background" Value="{StaticResource TTEditorActiveCursorLine3}"/> 
                <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}"/> 
            </DataTrigger>
            <DataTrigger Binding="{Binding _flag}" Value="I">
                <Setter Property="Background" Value="{StaticResource TTEditorActiveCursorLine}"/> 
                <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}"/> 
            </DataTrigger>
            <DataTrigger Binding="{Binding _flag}" Value="S">
                <Setter Property="Background" Value="{StaticResource TTEditorActiveCursorLine2}"/> 
                <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}"/> 
            </DataTrigger>
            <DataTrigger Binding="{Binding _flag}" Value="C">
                <Setter Property="Background" Value="{StaticResource TTEditorActiveCursorLine3}"/> 
                <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}"/> 
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <!-- misc -->
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}" />
    </Style>
    <Style TargetType="StatusBarItem">
        <Setter Property="Background" Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}" />
    </Style>
    <Style TargetType="DockPanel">
        <Setter Property="Background" Value="{StaticResource BaseColor}" />
        <Setter Property="LastChildFill" Value="True" />
    </Style>
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}" />
    </Style>
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}" />
    </Style>
    <Style TargetType="TextBox">
        <Setter Property="FontSize" Value="10" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="{StaticResource BaseColor}" />
        <Setter Property="Foreground" Value="{StaticResource TextEditor_Base_Foreground}" />
        <Setter Property="ContextMenu" Value="{x:Null}" />
    </Style>
    <Style TargetType="Label">
        <Setter Property="FontSize"     Value="10" />
        <Setter Property="FontWeight"   Value="Bold" />
        <Setter Property="FontFamily"   Value="Meiryo" />
        <Setter Property="Background"   Value="{StaticResource Label_Title_Background}" />
        <Setter Property="Foreground"   Value="{StaticResource Label_Title_Foreground}" />
        <Setter Property="Margin"       Value="0,0,4,0" />
        <Setter Property="VerticalAlignment"    Value="Bottom" />
        <Setter Property="Padding"              Value="2,2,2,2" />
        <Setter Property="HorizontalAlignment"  Value="Stretch" />
    </Style>


</ResourceDictionary>

