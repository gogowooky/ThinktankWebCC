# P3 続き：AIChatへの資料付き対話の接続

実装日：2026-09-14。

## 背景と今回の範囲

新しい引用付き対話はOverviewのBundle状況に実装済みだったが、各AIChatは旧履歴の閲覧専用で入力欄がなかった。日常の相談入口を整えるため、P6の資料同期へ進む前にP3の画面接続を追加した。

Thinktank・Overview・Workout・ReThinkのAIChat、およびWorkoutのChatペインが共用するSupportChatに、Bundleを根拠とする対話を接続した。

2026-09-15のUI整理で、AIChat内に追加していた「AIに相談する」「これまでの会話」の切替と、重複するBundle選択リストを廃止した。各画面の上部にある既存のBundle選択を相談対象として使う。保存済みChatファイルとデータは残し、AIChatのファイル一覧には次の範囲で表示する。

- Thinktank：Vault全体を対象に、Chatタイトルの担当がThinktankのファイル、および従来仕様でThinktankへ配置される担当未分類のChatファイル。
- Overview・Workout・ReThink：Overview上部で選択中のBundleを対象に、Chatタイトルの担当がそれぞれのパネルと一致するChatファイル。
- OverviewでBundleを選択していない場合、Overview・Workout・ReThinkの一覧は空。Bundleを切り替えた場合、切替先に含まれない選択は解除する。

「これまでの会話」パネルは復元せず、Chatファイル本文をP3の対話履歴として読み込んだり更新したりしない。

## 利用手順

1. 各画面の上部にある既存リストでChatファイルを選ぶ。
2. AIChatを開く。ThinktankではChatを選択すれば、OverviewのBundleが未選択でも質問欄を表示する。Overview・Workout・ReThinkではChatと参照Bundleの両方が必要。両者のタイトル一致は前提にしない。
3. Chatを選ぶと、履歴・参照資料・接続状態を自動で準備する。通常画面には会話、入力欄、送信操作と必要なエラーだけを表示する。接続先や参照範囲、補助操作は「参照情報・その他」に収める。
4. 質問を入力し、AIへの送信チェック後に送信する。生成中断、保存の再試行、未保存回答のJSON書き出しは既存P3と共通。
5. 引用の出典ボタンでWorkoutに原本を開く。既存ペインがあればそこへ移動し、なければ追加する。

AIが停止中でもBigQueryモードの質問欄には入力できる。送信にはP3のAI設定が必要。Gemini File Searchの接続設定だけでは対話AIは有効にならない。今回、AIの有効化や認証設定の変更、実APIへの質問送信は行っていない。

## データと切替

- AIChatの保存先は上部で選択したChatファイルの`thinkConversations`。Bundleは資料Snapshotの作成に使い、Bundle側の`thinkConversations`は更新しない。構造化履歴から会話テキストをChat本文の管理区画へ同期するため、texteditorでも全会話を読める。既存の手書き本文と従来の管理メタデータを保全し、旧AI処理も再開しない。
- 保存先は選択したChat IDで決定する。ChatタイトルとBundleタイトルの一致、識別タグを除いたタイトルからのBundle検索は保存条件にしない。同名Chatを作る運用は将来の方針として扱う。
- ThinktankでBundleが未選択の場合は`chat-only`範囲として、参照資料・Bundle手動記録・引用なしでAIへ質問する。履歴は選択Chatから直近6往復を読み、回答も同じChatへ保存する。OverviewのBundleを選択している場合は、従来どおりそのBundleの資料Snapshotを利用する。
- AIChatは上部のChat選択に追従する。別Chatを選ぶと履歴の読込先と保存先も切り替わる。未選択、別Vault、一覧から消えたChat、または同名Bundleを特定できない場合は対話を表示しない。
- 質問下書き・未保存回答はVaultオブジェクト、Bundle、画面の識別子ごとに分離する。Overviewと各パネルが同じBundleを開いても下書きを上書きしない。対話履歴そのものは同じBundle・Vaultで共有する。
- 履歴表示への切替やBundle変更時にはP3の生成中断処理が働く。戻った際は対象Bundleを選び直すことで、同じ画面の質問・未保存回答を再表示できる。参照資料と送信確認は再取得する。
- 未保存回答を保存する際は、同じ回答が既に別の再試行で処理された場合に新しい下書きを消さないよう、保留中の回答IDを確認する。
- AI提案は未採用として表示し、採用先としてOverviewの手動編集を案内する。

## 検証

2026-09-15の変更後は、上部Chatへの追従、タイトルが異なる選択Chatへの履歴保存、ThinktankのBundleなし対話、選択後の自動準備、会話中心の簡潔な表示、Bundleレコードを更新しないこと、旧会話履歴パネルの非表示、対象不在時の送信抑止、既存Chat本文の保全をテストする。さらに、ThinktankのVault全体範囲、他3パネルのOverview Bundle範囲、Chatタイトルによる担当パネルの自動振り分け、Bundle外の選択を保持しないことを検証する。既存の停止中通信禁止、送信確認、中断、保存失敗時の再試行、画面ごとの下書き分離も維持する。

型検査、変更したTypeScriptファイルのESLint（エラー0）、全体51ファイル・365テスト、フロントエンド／サーバービルドが成功した。既存のLint警告、大きなチャンク警告と、TTApplicationの動的／静的import混在により別チャンクにはならない旨の警告がある。実AI・実BQ・実画面での受け入れは未実施。

## 次の段階

P6では指定済みのGemini File Searchを継続する。接続先の実証、Bundleとストアの永続対応、確認済みSnapshotの資料送信、成功分のハッシュ保存、部分失敗からの再開を実装する。File Searchによる検索回答と今回のP3対話の接続は、その引用対応を確認した上で進める。
