# P0〜P2：作業フォルダーへの統合と追加検証

実施日：2026-09-14。対象：`C:\Users\gogow\Documents\ThinktankWebCC`、開始時点 `506d325`（`TTWeb260526`）。

## Step 1：文書とソースの差の確認

設計文書にはP1・P2の実装済み記録があったが、この作業フォルダーのソースには未反映だった。`codex/ai-neutral-foundation` のAI-neutral化・P1、および同worktreeの未コミットP2実装を確認して再利用した。開始時点の `src`・`server` が共通基準 `e3f9fb9` と同一であることを照合し、対象40ファイルを統合した。既存の文書整理を巻き戻していない。元worktreeのファイルも変更していない。

## Step 2：AI停止と読み取り基盤

旧AI経路を停止し、旧会話・メタデータは閲覧できる形で保全。ContextServiceがBundle定義・入れ子・除外条件に従って資料を読み、SHA-256、出典、欠落・検索不完全・旧決定の競合をSnapshotへまとめる。

## Step 3：手動Overviewと保存

OverviewのBundle「この課題の状況」から、目的・段階・暫定結論・決定事項・残る論点・次の行動・完了条件を編集できる。出典を選択し、本人確認後にBigQueryへ保存する。本文や旧メタデータを保持し、保存版の競合を比較できる。AIキーは不要。

下書きはVault・Bundle単位でセッション中保持する。Electron／Localの手動保存は未対応。BigQueryモードで利用する。

## Step 4：統合時に修正した不具合

- 保存側の内容を採用した後も比較状態が残り、再保存できなくなる不具合を修正。採用後の編集・確認・保存を回帰テストで検証。
- 競合比較に出典ID・本文ハッシュ・本人確認日時を表示し、文章が同じでも根拠の変更を確認できるようにした。
- Overview下部の会話・資料一覧もContextService経由に統一。空Bundleで全Vaultを表示してしまう旧解決処理との不整合を解消。空Bundleと明示資料ありの2ケースをUIテストで検証。Vault切替時の結果識別も追加。

## Step 5：検証

- 統合時の全体回帰：29ファイル・234テスト成功。
- 追加修正後のOverviewテスト：2ファイル・8テスト成功（追加3件を含む）。
- フロント・サーバー型検査、両ビルド成功。フロントビルドはサンドボックスのディレクトリ読取制限で初回失敗後、許可された通常権限で成功。
- 既存の大きなJSチャンク警告は継続。
- 主要な統合・変更箇所のESLint：エラー0、既存の`any`警告3件。追加テストを含めた最終型検査も成功。

テストの保存先はモック。実BQの保存・再読込、実ブラウザ／Electronの操作、バックアップから検証先への復元は未実施。デプロイや実データ移行は実施していない。

## 次の段階

P0の復元・基本操作とP2の実BQ受け入れ確認を行う。P3は接続先・AI再開の確認後、既定停止のProvider境界と引用付き対話を追加する。P3〜P7は今回未実装。
